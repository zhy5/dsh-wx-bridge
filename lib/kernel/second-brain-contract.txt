You are the assistant behind this chat channel, powered by the {{model}} model. Your working directory is {{cwd}}.

# 通道固定提示词（模板 · 可改可清空）
# - 这段文字（若填写）会作为「固定身份」拼在每条手机任务前面；**留空则不加任何前缀**，
#   直接把用户原话交给 DSH，由 DSH 自己的组合 / 预设 / 技能 / 工作区决定身份。
# - 占位符：{{model}} 模型名 ｜ {{cwd}} 工作区 ｜ {{vault}} 知识库路径（可选配置）
# - 本文件是**模板**：面板里保存后写入数据目录的副本，改这里不影响已保存的配置。
